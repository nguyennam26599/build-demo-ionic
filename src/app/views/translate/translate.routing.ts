import { Routes } from "@angular/router";
import { TranslateComponent } from "./translate/translate.component";

export const TranslateRouting: Routes = [
    {
        path: 'translate',
        component: TranslateComponent
    }
]